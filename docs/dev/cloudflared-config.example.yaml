# Example `cloudflared` config for exposing OpenRails locally at a stable hostname.
#
# Usage (example):
# - Save as: ~/.cloudflared/openrails-webhooks.yaml
# - Run: cloudflared tunnel --config ~/.cloudflared/openrails-webhooks.yaml run <tunnel-name>
#
# DO NOT COMMIT real tunnel credentials/tokens.
#
# Docs: docs/dev/local-webhooks.md

tunnel: <tunnel-uuid-or-name>
credentials-file: /path/to/<tunnel-uuid>.json

ingress:
  - hostname: openrails-webhooks-sandbox.<your-domain>
    service: http://localhost:3053
  - service: http_status:404
