{
  "issues": [
    {
      "id": 133,
      "name": "admin-analytics-and-billing-dashboard",
      "description": "Build an internal admin web UI for billing analytics and support operations.\n\n## Metadata\n\n- Category: product\n- Status: future\n- Passes: false\n\n## Goals\n\n- Primary: analytics visibility (sales, churn, failures, revenue).\n- Secondary: safe admin operations (support workflows) with audit trails.\n\n## Non-goals (initially)\n\n- Customer-facing self-serve dashboard.\n- Replacing core billing APIs.\n\n## Notes\n\n- Keep the surface area minimal: read-heavy analytics first, then a small set of high-confidence mutations.\n- Every mutation must be gated + audited (who/what/when/why).",
      "tasks": [
        "FOUNDATION:",
        "[ ] Choose hosting path (serve via billing service, or separate static app + API calls)",
        "[ ] Define admin authz model (role/entitlement) and enforce on all dashboard APIs",
        "[ ] Add structured audit log table/events for admin actions (actor, action, target, before/after, request_id)",
        "",
        "ANALYTICS (READ):",
        "[ ] Define core metrics: gross sales, net sales, refunds, chargebacks, MRR, active subs, churn, failed attempts",
        "[ ] Implement time-series queries (today, 7d, 30d) with timezone handling",
        "[ ] Add breakdowns: processor (NMI/CCBill/Solana), product/tier, country (if available)",
        "[ ] Add payments funnel: attempts -> succeeded -> refunded/chargeback -> net",
        "[ ] Add dunning visibility: past_due count, retries, recovered vs churned",
        "",
        "ADMIN OPS (MUTATIONS):",
        "[ ] User lookup + detail view (subscriptions, payments, entitlements, processor IDs)",
        "[ ] Support actions: cancel subscription, pause/resume, extend access, comp/refund (only where safe)",
        "[ ] Guardrails: confirmation steps, rate limiting, and require a reason for every mutation",
        "[ ] Safe idempotency for all admin operations (avoid double-cancel/refund)",
        "",
        "API + DATA LAYER:",
        "[ ] Add dedicated admin endpoints (e.g. /v1/admin/analytics/*, /v1/admin/users/*)",
        "[ ] Add indexes/materialized views if needed for dashboard performance",
        "[ ] Add caching strategy for expensive aggregates (optional)",
        "",
        "UI:",
        "[ ] Minimal dashboard pages: Overview, Payments, Subscriptions, Dunning, User Lookup",
        "[ ] Tables: filter/sort/paginate; export CSV for key views",
        "[ ] Detail drilldowns from charts -> filtered tables",
        "",
        "VERIFICATION:",
        "[ ] Unit tests for analytics queries (edge dates/timezones) and authz checks",
        "[ ] Integration tests for a small set of admin mutations + audit logging",
        "[ ] Manual checklist for production access controls and least privilege"
      ],
      "completed": false
    },
    {
      "id": 134,
      "name": "usage-based-billing (postpaid metering + invoicing)",
      "description": "Add first-class support for usage-based (metered) billing where users consume resources first and are billed after the fact.\n\nToday, doujins-billing primarily supports prepaid-style charging (subscriptions and one-off payments) plus a credits/balance system that can be decremented at time-of-use. That covers many \"usage\" cases as prepaid credits.\n\nThis issue is specifically about **postpaid** usage: record usage events, aggregate them into an invoice/statement for a billing period, and collect payment after the usage occurred.\n\n## Stripe Billing Meter API (2025+)\n\nStripe deprecated the legacy usage records API (removed in API version 2025-03-31). The new approach uses the **Billing Meter API**:\n\n1. **Meters** - Define how to aggregate usage (sum, count, last_during_period)\n2. **Meter Events** - Individual usage records sent to Stripe (POST /v1/billing/meter_events)\n3. **Metered Prices** - Prices that link to meters (per-unit or tiered)\n4. **Subscriptions** - Connect customers to metered prices\n5. **Invoices** - Stripe auto-generates at period end based on aggregated meter events\n\n### Stripe Meter Event Structure\n```json\n{\n  \"event_name\": \"api_requests\",\n  \"payload\": {\n    \"stripe_customer_id\": \"cus_xxx\",\n    \"value\": \"150\"\n  },\n  \"identifier\": \"unique-idempotency-key\",\n  \"timestamp\": 1234567890\n}\n```\n\n### Rate Limits\n- Standard API: 1,000 events/second\n- V2 Meter Event Stream: 10,000 events/second (requires session auth)\n- Events must be within 35 days past or 5 minutes future\n\n## Goals\n\n- Support metered products (e.g. $/image, $/minute, $/GB) billed in arrears.\n- Align with Stripe's Billing Meter API as the primary integration path.\n- Keep internal usage tracking for audit/reporting (mirror events locally).\n- Make usage accounting auditable and idempotent (no double-billing).\n\n## Non-goals (initially)\n\n- Real-time streaming analytics dashboard.\n- Non-Stripe processor support for metered billing.\n\n## Design sketch\n\n### Data model (internal mirror)\n- `billing.meters` - Mirror of Stripe meters: {id, stripe_meter_id, event_name, display_name, aggregation, created_at}\n- `billing.meter_events` (append-only): {id, meter_id, user_id, stripe_customer_id, value, occurred_at, identifier, synced_to_stripe_at, error?}\n- `billing.metered_subscriptions` - Link users to Stripe subscriptions with metered prices\n\n### Flow\n1. Host app calls `RecordUsage(user_id, meter_name, value, idempotency_key)`\n2. doujins-billing stores event locally in `meter_events`\n3. doujins-billing forwards to Stripe via `POST /v1/billing/meter_events`\n4. Mark `synced_to_stripe_at` on success, store error on failure\n5. At billing period end, Stripe aggregates and generates invoice automatically\n6. Webhook `invoice.paid` / `invoice.payment_failed` updates subscription status\n\n### Stripe Setup (one-time)\n1. Create Meter in Stripe (event_name, aggregation: sum)\n2. Create Product with metered Price linked to Meter\n3. When user enables arrears billing, create Subscription with metered price\n\n## Open questions\n\n- Do we want spend caps / kill-switch when usage exceeds a threshold? (Stripe supports this)\n- How do we handle users without Stripe customer? (Require Stripe for arrears billing)\n- Retry strategy for failed Stripe meter event syncs?\n\n## References\n\n- https://docs.stripe.com/api/billing/meter\n- https://docs.stripe.com/api/billing/meter-event\n- https://docs.stripe.com/billing/subscriptions/usage-based/recording-usage-api\n- https://docs.stripe.com/billing/subscriptions/usage-based/implementation-guide\n",
      "tasks": [
        "STRIPE SETUP:",
        "[ ] Create Stripe Meters for each usage type (api_requests, gpu_seconds, tokens, etc.)",
        "[ ] Create metered Prices linked to each Meter",
        "[ ] Document meter event_name conventions",
        "",
        "DATA MODEL:",
        "[ ] Add migrations for meters + meter_events tables (internal mirror)",
        "[ ] Add metered_subscriptions table to link users to Stripe metered subscriptions",
        "[ ] Ensure idempotency via unique (meter_id, identifier) constraint",
        "",
        "API SURFACE:",
        "[ ] Add RecordUsage(user_id, meter_name, value, idempotency_key, timestamp?) method",
        "[ ] Sync to Stripe via POST /v1/billing/meter_events",
        "[ ] Add retry/queue for failed Stripe syncs (River job)",
        "[ ] Add GET /v1/me/usage endpoint for user to see current period usage",
        "",
        "SUBSCRIPTION MANAGEMENT:",
        "[ ] Add endpoint to enable arrears billing for a user (creates Stripe subscription with metered price)",
        "[ ] Handle invoice.paid webhook to confirm payment",
        "[ ] Handle invoice.payment_failed webhook (dunning, disable access?)",
        "",
        "HIGH VOLUME (OPTIONAL):",
        "[ ] Implement V2 Meter Event Stream for >1000 events/sec",
        "[ ] Batch local events and sync periodically instead of real-time",
        "",
        "TESTS + DOCS:",
        "[ ] Integration tests: record usage, verify Stripe sync, invoice generation",
        "[ ] Document how host apps should call RecordUsage"
      ],
      "completed": false
    },
    {
      "id": 135,
      "name": "auto-topup (low-balance automatic credit purchases)",
      "description": "Add an optional per-user auto-topup feature for prepaid credits.\n\nUse case: a user opts in to auto-buy more API credits when their balance drops below a threshold (e.g. below $5, automatically purchase enough to reach $100).\n\nThis is not postpaid metering; it is **prepaid auto-replenishment**.\n\n## Goals\n\n- Users can opt in/out and set:\n  - `threshold_cents` (trigger when balance <= threshold)\n  - `target_cents` or `topup_amount_cents` (how much to buy)\n  - `price_id` (which credits package / product to purchase)\n  - optional safety caps (e.g. max topups per day/week/month)\n- Topups are idempotent and safe under concurrency (no double-charging).\n- Works with whichever processor supports one-off purchases for that user (Stripe/NMI/etc), but the internal model is processor-agnostic.\n\n## Non-goals (initially)\n\n- Complex dynamic pricing or tiered metering.\n- Automatic retries forever; we should stop after a bounded number of failures and notify the user.\n\n## Design sketch\n\n- Data model:\n  - `billing.auto_topups` (per user): {id, user_id, enabled, price_id, threshold_cents, target_cents, max_per_day?, max_per_week?, last_triggered_at?, created_at, updated_at}\n  - `billing.auto_topup_events` (append-only): {id, auto_topup_id, user_id, observed_balance_cents, triggered_at, status, payment_id?, error_code?, error_message?}\n  - Reuse existing `billing.payments` as the purchase record; entitlements/credits granted are sourced from that payment.\n\n- Triggering:\n  - Option A (recommended): piggyback on credit spend paths.\n    - When credits are withdrawn/captured, check if balance <= threshold and enqueue an auto-topup job.\n  - Option B: scheduled worker that scans for low-balance users (requires indexes and careful load control).\n\n- Worker:\n  - Load user config and current balance.\n  - If balance > threshold or auto-topup disabled, no-op.\n  - Compute purchase amount (either fixed package via `price_id` or \"to target\" semantics if supported).\n  - Perform purchase using the same internal checkout/payment service used for one-off credit purchases.\n  - Idempotency key should include: user_id + auto_topup_id + \u201cbalance_snapshot\u201d/period bucket.\n  - On success: create payment + credit grant, record auto_topup_event.\n  - On failure: record event, increment failure counters, disable auto-topup after N consecutive failures, and notify user.\n\n- Safety:\n  - Enforce rate limits/caps (max topups per time window).\n  - Require a valid payment method on file / processor customer.\n  - Never run for users on unpaid/past_due subscription states unless explicitly allowed.\n\n",
      "tasks": [
        "FOUNDATION:",
        "[ ] Define user-facing settings (threshold, target, price selection, caps) and defaults",
        "[ ] Decide trigger strategy: spend-hook enqueue vs periodic scanner (prefer spend-hook)",
        "",
        "DATA MODEL:",
        "[ ] Add migrations for auto_topups + auto_topup_events tables",
        "[ ] Add indexes for enabled lookups and rate-limit enforcement (by user_id, triggered_at)",
        "",
        "WORKERS + IDP:",
        "[ ] Implement River worker to execute an auto-topup purchase with a strict idempotency key",
        "[ ] Implement safety caps (max per day/week/month) and disable-on-failure behavior",
        "",
        "INTEGRATION:",
        "[ ] Hook into credits spend/capture path to enqueue auto-topup checks",
        "[ ] Ensure topups create a normal Payment and grant credits sourced from that payment (no special casing)",
        "",
        "TESTS + DOCS:",
        "[ ] Integration tests for: trigger threshold, idempotency (double enqueue), caps, and failure disablement",
        "[ ] Document the user/admin UX and operational guardrails"
      ],
      "completed": false
    },
    {
      "id": 201,
      "name": "multi-tenant SaaS deployment of OpenRails (tenant-per-DB, scaling ladder)",
      "description": "Evolve OpenRails from a single-tenant service (one OpenRails process per customer org) into a hosted SaaS platform where multiple paying tenants share infrastructure. The motivation: doujins, hentai0, and similar host apps will want a managed billing backend so they don't have to run their own Stripe-backed billing service. We host one OpenRails platform; each tenant gets isolated billing data, isolated Stripe credentials, and isolated webhooks.\n\n## Relationship to single-tenant OpenRails\n\nOpen-source OpenRails stays single-tenant in data model (no `org_id` columns on billing tables). Issues #205 and #206 explicitly bake in the rule that open-source OpenRails serves one billing catalog per deployment. **This issue intentionally uses the same code with no fork \u2014 multi-tenancy is achieved by deployment topology, not by patching OpenRails.**\n\n## Chosen approach: tenant-per-DB, tenant-per-pod\n\nAfter comparing tenant-per-DB / in-core multi-tenant flag / separate fork: the SaaS uses **tenant-per-DB**, with one OpenRails process per tenant pointed at its own database. No code changes to OpenRails. Isolation is by deployment topology; one tenant's data and one tenant's Stripe credentials are physically separate from another's.\n\nOperational shape at the small-tenant scale we expect (single-digit to a few dozen tenants):\n\n- One Postgres container/cluster hosting many databases \u2014 one DB per tenant (`tenant_<slug>`).\n- One OpenRails pod per tenant on Kubernetes, each configured with its own `DATABASE_URL`, Stripe credentials, and webhook signing secret.\n- One PgBouncer in front of Postgres in transaction mode, with per-DB pool slots sized for tenant count \u00d7 per-tenant concurrency.\n- Per-tenant secrets in Vault under namespaced paths.\n- An ingress router that maps inbound HTTP / webhook URLs to the correct tenant pod (path-based: `/t/<tenant_slug>/...` or subdomain-based: `<tenant>.platform.example`).\n\n## Scaling ladder (when to revisit the choice)\n\nThe chosen approach has a built-in ceiling. Document the ladder so it's not surprising when we hit it:\n\n- **2\u201350 tenants** (target range): one Postgres cluster + N DBs + N OpenRails pods + PgBouncer. Simple, cheap, isolated.\n- **50\u2013500 tenants**: the real bottleneck is connection count, not DB count. PgBouncer needs careful per-tenant sizing; per-DB autovacuum / migration runs become a fleet operation; 500 idle OpenRails pods get wasteful. At this scale consider (a) schema-per-tenant in one DB (same data isolation, one connection pool, one vacuum cycle) or (b) cluster-per-cohort sharding.\n- **500+ tenants**: tenant-per-pod becomes operationally awful. You either go to row-level multi-tenancy with `org_id` (the in-core flag approach that this issue explicitly rejected) or accept cluster-per-cohort sharding plus tenant-routing logic in a separate ingress process.\n\nThe ladder is a forcing function: at ~few hundred tenants, the architectural decision must be revisited. Until then, the tenant-per-DB simplicity wins.\n\n## Why this beats the alternatives at our expected scale\n\n- **vs separate fork** (was the original framing): no merge debt with OSS releases; every OpenRails security fix and feature lands automatically.\n- **vs in-core multi-tenant flag**: OSS users never see tenancy code; we never have to test cross-tenant scope leaks; no risk of forgetting a `WHERE tenant_id = ?` clause; Postgres handles isolation for us.\n- **vs separate Postgres container per tenant**: shared container amortizes Postgres overhead (~150MB base RAM each), shared backup pipeline, shared monitoring; only the data and connection paths are isolated.\n\n## Goals\n\n- Multiple tenants share one platform with strong isolation (data, secrets, webhooks, processor credentials).\n- Per-tenant Stripe credentials (each tenant brings their own Stripe account or uses Stripe Connect under our platform account).\n- Per-tenant webhook routing and signature verification.\n- Operational tooling to provision / suspend / delete tenants and audit cross-tenant actions.\n- Compliance primitives: per-tenant data export and deletion.\n- The OSS OpenRails repo does not gain tenant-aware code paths; the SaaS is a deployment platform around unmodified OpenRails images.\n\n## Non-goals\n\n- In-core `tenant_id` scoping in OpenRails itself. If we ever need it (>500 tenants), it's a fresh issue.\n- A separate private fork of OpenRails. If we hit a missing primitive, we add it upstream to OSS OpenRails.\n- Reinventing tenant IAM; lean on AuthKit's org concept (an OpenRails SaaS tenant maps 1:1 to an AuthKit org, with that org's admins being the SaaS-side billing admins; this matches issue #205's `OPENRAILS_OPERATOR_ORG_SLUG` config per tenant pod).\n- Replacing OpenRails's single-tenant runtime semantics for the embedded use case; this platform is a deployment shape, not a replacement.\n\n## Key open questions\n\n- Processor credentials: each tenant brings their own Stripe account (BYO) vs Stripe Connect under our platform account vs both?\n- Webhook routing: per-tenant subdomains (`tenant.platform.example/webhooks/stripe`) vs path-based (`/t/<tenant>/webhooks/stripe`) \u2014 both work with the ingress-router shape; subdomain feels cleaner for Stripe Dashboard configuration.\n- Billing the tenants: how the platform charges its tenants \u2014 flat per-tenant fee, percentage of volume, tiered, or a separate SKU? (Probably the platform should dogfood OpenRails \u2014 run an OpenRails instance to bill its own tenants.)\n- Backup / restore granularity: per-DB `pg_dump` is fine for routine; PITR via WAL is cluster-wide so a single-tenant point-in-time-restore needs pgBackRest stanza-per-DB or a logical-replication side stream. Decide at provisioning time.",
      "tasks": [
        "RFC + ARCHITECTURE:",
        "[ ] Publish the chosen-approach RFC documenting: tenant-per-DB + tenant-per-pod, why approach 2 (in-core flag) and approach 3 (fork) were rejected at this scale, the scaling ladder, and the explicit revisit point at ~few hundred tenants",
        "[ ] Document AuthKit-org-to-SaaS-tenant mapping: one AuthKit org = one SaaS tenant (1:1)",
        "[ ] Decide processor-credentials model: tenant BYO Stripe / Stripe Connect / both",
        "[ ] Decide webhook routing strategy: subdomain (`<tenant>.platform.example/webhooks/...`) vs path (`/t/<tenant>/webhooks/...`)",
        "[ ] Decide billing model for how the platform charges tenants (and decide if the platform dogfoods OpenRails for its own billing)",
        "",
        "INFRASTRUCTURE (POSTGRES + INGRESS):",
        "[ ] Provision one shared Postgres cluster with naming convention `tenant_<slug>` for per-tenant databases",
        "[ ] Stand up PgBouncer in transaction mode in front of Postgres; size pool slots for tenant count \u00d7 per-tenant concurrency",
        "[ ] Tune `max_connections`, shared_buffers, work_mem with the tenant-count headroom in mind (each pooled connection ~10MB)",
        "[ ] Build the ingress router: maps incoming HTTP / webhook URL \u2192 tenant slug \u2192 tenant OpenRails pod",
        "[ ] Configure DNS / cert-manager for the chosen routing strategy (subdomain or path)",
        "[ ] Document Postgres connection-budget calculator (pods \u00d7 per-pod connections vs PgBouncer pool size vs Postgres max_connections)",
        "",
        "TENANT PROVISIONING:",
        "[ ] Tenant provisioning API: create-tenant takes AuthKit org id, processor credentials reference, billing tier; emits tenant slug + Vault paths",
        "[ ] Provisioning script/job that runs in order: (1) create Postgres DB, (2) run OpenRails migrations on the new DB, (3) seed initial AuthKit-org config, (4) store secrets in Vault, (5) deploy K8s pod for tenant, (6) register ingress routes, (7) smoke-test tenant pod health",
        "[ ] Per-tenant `DATABASE_URL`, `BILLING_STRIPE_SECRET_KEY`, `BILLING_STRIPE_WEBHOOK_SECRET`, `OPENRAILS_OPERATOR_ORG_SLUG` injected from Vault at pod start",
        "[ ] Tenant onboarding flow: link AuthKit org, attach Stripe credentials (BYO or Connect), verify webhook delivery, seed catalog",
        "",
        "TENANT LIFECYCLE (POST-PROVISIONING):",
        "[ ] Tenant suspension: pause the tenant pod's writes (read-only mode or scale-to-zero with maintenance page), deny admin mutations, preserve historical data",
        "[ ] Tenant deletion: full data purge with confirmation gate \u2014 drop Postgres DB, delete Vault secrets, tear down K8s pod, deregister ingress; export-before-delete enforced",
        "[ ] Tenant data export: per-DB `pg_dump` + Vault-side secret enumeration zipped for GDPR / portability requests",
        "[ ] Tenant tier change: upgrade/downgrade the platform's own billing for this tenant (eats own dogfood)",
        "[ ] Per-tenant migration runner: on OpenRails release, walk every tenant DB and run migrations; report per-tenant success/failure; halt fleet rollout on schema drift",
        "",
        "PROCESSOR CREDENTIAL MANAGEMENT:",
        "[ ] Per-tenant Stripe credential storage in Vault (path namespaced by tenant slug)",
        "[ ] Stripe Connect onboarding flow if Connect is in scope (`accounts.create`, hosted onboarding link, account status webhooks)",
        "[ ] Credential rotation API + audit log",
        "[ ] Per-tenant credential test endpoint (verify Stripe key works without charging)",
        "",
        "WEBHOOK ROUTING:",
        "[ ] Per-tenant webhook signing secret storage in Vault",
        "[ ] Ingress router resolves tenant from webhook URL \u2192 forwards to that tenant's pod",
        "[ ] Per-tenant signature verification happens inside the tenant pod (existing OpenRails logic), not in the router \u2014 the router is transport-only",
        "[ ] Webhook delivery monitor per tenant (alert on >N consecutive failures)",
        "[ ] Document how each tenant configures their Stripe Dashboard webhook URL (subdomain or path)",
        "",
        "AUTH + ADMIN:",
        "[ ] Each tenant pod sets `OPENRAILS_OPERATOR_ORG_SLUG=<tenant_authkit_org>` so issue #205's admin policy gates `/admin/*` correctly per tenant",
        "[ ] Platform-superadmin policy (separate from tenant admin): a small set of identities can administer any tenant via the control plane (support / compliance / incident response)",
        "[ ] Audit log: every cross-tenant action by a platform-superadmin is logged with actor, target tenant, reason; lives outside any tenant DB (in the control-plane DB)",
        "[ ] Break-glass access flow: time-boxed, requires written justification, alerts on use",
        "",
        "CONTROL PLANE:",
        "[ ] Control-plane DB (separate from tenant DBs): tenant directory, audit logs, provisioning state, platform billing",
        "[ ] Platform admin API: list tenants, inspect tenant pod health, search across tenants (with audit), provision/suspend/delete",
        "[ ] Platform admin UI: tenant directory, per-tenant drilldown, provisioning wizard, suspension control, audit log viewer",
        "[ ] Platform metrics: tenant count, per-tenant revenue, churn, webhook failure rates, dunning recovery \u2014 aggregated from tenant DBs via a read-only fan-out query, or via per-tenant emitted events into a central analytics store",
        "",
        "OBSERVABILITY + OPERATIONAL:",
        "[ ] Per-tenant log/metric labels (tenant slug as label) so traces and dashboards filter by tenant",
        "[ ] Per-tenant rate limits / quotas (API requests, webhook ingest, storage) \u2014 enforce in the ingress router",
        "[ ] Per-tenant backup strategy: routine per-DB `pg_dump` on a schedule; for PITR, run pgBackRest with stanza-per-DB or accept cluster-wide PITR with per-DB restore via logical replication side stream",
        "[ ] Connection-pool monitoring (PgBouncer stats per DB, alert on saturation)",
        "[ ] Postgres autovacuum + statistics monitoring across the fleet (autovacuum scales with total relation count = sum across all tenant DBs)",
        "[ ] On-call runbook: per-tenant incident triage, scoped break-glass, blast-radius assessment, tenant-pod restart playbook",
        "",
        "BILLING THE TENANTS (SaaS revenue):",
        "[ ] Platform-side product catalog (the SaaS plans we sell to tenants)",
        "[ ] If dogfooding OpenRails for platform billing: stand up a 'control-plane OpenRails' instance with its own DB inside the same Postgres cluster",
        "[ ] Tenant signup + plan selection flow",
        "[ ] Platform's own Stripe account / Stripe Connect platform setup",
        "[ ] Usage metering if the platform charges by volume (depends on future #134)",
        "[ ] Tenant payment failure \u2192 tenant suspension flow",
        "",
        "COMPLIANCE + SECURITY:",
        "[ ] Data residency story (per-tenant region pinning if EU customers require it \u2014 affects which Postgres cluster their DB lives in)",
        "[ ] Per-tenant encryption-at-rest keys (envelope encryption with tenant-specific DEKs; Postgres TDE or app-level field encryption)",
        "[ ] Penetration test scoped to cross-tenant data leakage before GA (ingress router bypass, PgBouncer cross-DB confusion, webhook misrouting)",
        "[ ] DPA template for tenants",
        "[ ] SOC 2 / ISO 27001 readiness assessment",
        "",
        "SCALING CHECKPOINTS (forced revisits):",
        "[ ] At 25 tenants: review PgBouncer pool sizing, connection budget, per-tenant pod resource footprint",
        "[ ] At 100 tenants: review whether schema-per-tenant or cluster-per-cohort sharding is needed; revisit per-tenant pod cost",
        "[ ] At 300 tenants: decide whether to file a new issue for in-core multi-tenancy (row-level `tenant_id`) or commit to cluster-per-cohort sharding",
        "",
        "EXIT CRITERIA:",
        "[ ] RFC published and approved with the tenant-per-DB / tenant-per-pod approach + scaling ladder documented",
        "[ ] A tenant can be provisioned, charged, refunded, and deleted end-to-end through the platform admin API + UI",
        "[ ] Two real tenants run on the platform in production without sharing data, secrets, or webhook routes",
        "[ ] OSS OpenRails repo is unchanged; tenant pods run unmodified OpenRails container images",
        "[ ] Per-tenant migration runner survives a release with at least one simulated mid-migration tenant failure (rollback works)",
        "[ ] Audit log covers every platform-superadmin cross-tenant action",
        "[ ] Documented runbooks for tenant lifecycle (provision/suspend/delete/export), incident response, break-glass access, and the connection-budget calculator"
      ],
      "completed": false
    },
    {
      "id": 202,
      "name": "admin-credit-blocks (manual grant + revoke by block)",
      "description": "Add a minimal admin surface for managing user credits as immutable \"blocks\":\n\n- Admin can **create** a credit block (grant credits, optional expiry)\n- Admin can **delete** a credit block (revoke the remaining credits from that specific grant)\n\nThis intentionally avoids a full taxonomy (refund/admin_adjust/etc.). The goal is a small, safe API that maps to support workflows.\n\n## Metadata\n\n- Category: admin\n- Status: future\n- Passes: false\n\n## Goals\n\n- Minimal mutation surface: create block, delete block.\n- Revocation is scoped to a specific block/grant, not \"take N credits from the user\".\n- Strong consistency under concurrency (holds, captures, withdrawals, and block revocation).\n- Clear auditability: who/what/when/why for every admin mutation.\n\n## Non-goals (initially)\n\n- Editing blocks (no partial edits or amount changes).\n- Automatically interpreting payment refunds as credit revocations.\n- Allowing negative balances (clawbacks) unless explicitly added later.\n\n## Design sketch\n\n### Data model\n\nToday, revocation-by-block is hard because only expiring deposits create `billing.credit_expiry_batches`, and withdrawals consume FIFO by `expires_at`.\n\nIntroduce an explicit \"block\" record so we can revoke remaining credits precisely:\n\n- New table: `billing.credit_blocks`\n  - {id, user_id, credit_type_id, original_amount, remaining_amount, expires_at NULL, source, source_id UUID NULL, created_by, revoke_reason, revoked_at, created_at, updated_at}\n  - Unique/indexes: (user_id, credit_type_id, expires_at), (user_id, credit_type_id, revoked_at IS NULL)\n\nConsumption rule:\n- On withdrawal/capture, decrement `credit_blocks.remaining_amount` in deterministic order:\n  - Prefer earliest `expires_at` first, then oldest created.\n  - Treat NULL expires as \"never\", ordered last.\n\nBalances rule:\n- Keep `billing.user_credit_balances` as the fast denormalized read.\n- All mutations that affect blocks also update `balance` in the same DB transaction.\n\n### APIs\n\nPrivate admin/service routes (X-API-KEY):\n- `POST /v1/admin/credit-blocks` create a block\n- `DELETE /v1/admin/credit-blocks/:id` revoke remaining credits from that block\n- (Optional but likely needed) `GET /v1/admin/credit-blocks?user_id=&type=` list blocks for a user\n\n### Semantics for delete/revoke\n\n- Default: revoke only the block's `remaining_amount` at time of deletion.\n- If the block has been partially spent, deletion still succeeds and just removes what remains.\n- Guardrail: do not allow revocation that would make `balance < held_balance`.\n  - Return 409 with guidance to release/expire holds first.\n\n## Integration points\n\n- `internal/services/credits_service.go`:\n  - Add a block-backed accounting path for deposits/withdrawals/captures.\n  - Keep existing ledger writes to `billing.credit_transactions` for observability.\n- `internal/river/jobs_credit_expiry.go`:\n  - If blocks have expiries, expiry worker should zero `remaining_amount` and reduce balance (analogous to existing batches).\n  - Or keep expiry as a direct block operation without `credit_expiry_batches`.\n\n## Exit Criteria\n\n- Admin can grant credits with optional expiry as blocks.\n- Admin can delete a block and the system removes only the remaining credits from that grant.\n- All operations are race-safe with holds/capture/withdrawal.\n- Tests cover concurrency and edge cases (partial spend + revoke, holds present, expiry).",
      "tasks": [
        "DATA MODEL:",
        "[ ] Add migration for billing.credit_blocks table + indexes",
        "[ ] Decide how to map existing credit_expiry_batches (keep, migrate, or deprecate)",
        "",
        "SERVICE METHODS:",
        "[ ] Add CreateCreditBlock(user_id, type, amount, expires_at?, reason, source?)",
        "[ ] Add RevokeCreditBlock(block_id, reason) that removes remaining_amount and marks revoked_at",
        "[ ] Update withdrawal/capture logic to consume from blocks deterministically (expiry first)",
        "[ ] Enforce guardrail: prevent balance dropping below held_balance on revoke/expiry",
        "",
        "HTTP API:",
        "[ ] Add private admin endpoints under /v1/admin (create/delete/list blocks)",
        "[ ] Return stable IDs for blocks and expose remaining/expiry info",
        "",
        "WORKERS:",
        "[ ] Implement expiry worker for credit_blocks (or adapt existing credit expiry worker)",
        "",
        "AUDITABILITY:",
        "[ ] Plumb through actor + reason fields from admin requests",
        "[ ] Add structured audit log event per mutation (optional table if not already present)",
        "",
        "TESTS:",
        "[ ] Integration tests: create block -> spend -> revoke -> verify balances and remaining",
        "[ ] Integration tests: revoke blocked by holds (balance < held_balance)",
        "[ ] Concurrency test: withdrawal and revoke racing (no double spend, no negative remaining)"
      ],
      "completed": false
    }
  ]
}
