// Module boundary marker: excludes the npm tree (node_modules ships stray .go
// files) from the parent module's ./... patterns. Not a real Go module.
module github.com/open-rails/openrails/web/admin

go 1.26
