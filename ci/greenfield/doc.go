// Package greenfield contains the compact API contract suite. Integration
// scenarios are build-tagged so ordinary package checks can still enumerate
// and compile the package without requiring PostgreSQL.
package greenfield
