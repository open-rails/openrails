// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: payment_provider_accounts.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countActiveProviderAccountsForNewWork = `-- name: CountActiveProviderAccountsForNewWork :one
SELECT count(*)::bigint FROM openrails.payment_provider_accounts
WHERE merchant_id = $1::uuid
  AND rail = lower($2::text)
  AND environment = COALESCE($3::text, 'live')
  AND archived = false
`

type CountActiveProviderAccountsForNewWorkParams struct {
	MerchantID  uuid.UUID
	Rail        string
	Environment *string
}

func (q *Queries) CountActiveProviderAccountsForNewWork(ctx context.Context, arg CountActiveProviderAccountsForNewWorkParams) (int64, error) {
	row := q.db.QueryRow(ctx, countActiveProviderAccountsForNewWork, arg.MerchantID, arg.Rail, arg.Environment)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const countProviderAccountsForRailEnvironment = `-- name: CountProviderAccountsForRailEnvironment :one
SELECT count(*)::bigint FROM openrails.payment_provider_accounts
WHERE merchant_id = $1::uuid
  AND rail = lower($2::text)
  AND environment = COALESCE($3::text, 'live')
`

type CountProviderAccountsForRailEnvironmentParams struct {
	MerchantID  uuid.UUID
	Rail        string
	Environment *string
}

func (q *Queries) CountProviderAccountsForRailEnvironment(ctx context.Context, arg CountProviderAccountsForRailEnvironmentParams) (int64, error) {
	row := q.db.QueryRow(ctx, countProviderAccountsForRailEnvironment, arg.MerchantID, arg.Rail, arg.Environment)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const getActiveProviderAccountForNewWork = `-- name: GetActiveProviderAccountForNewWork :one
SELECT id, merchant_id, rail, environment, account_id, display_name, vault_secret_ref, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, owner, archived FROM openrails.payment_provider_accounts
WHERE merchant_id = $1::uuid
  AND rail = lower($2::text)
  AND environment = COALESCE($3::text, 'live')
  AND archived = false
ORDER BY created_at DESC, id DESC
LIMIT 1
`

type GetActiveProviderAccountForNewWorkParams struct {
	MerchantID  uuid.UUID
	Rail        string
	Environment *string
}

// The newest non-archived account on a rail+environment. Existing provider-bound
// work must use its recorded provider_account_id instead of this selector.
func (q *Queries) GetActiveProviderAccountForNewWork(ctx context.Context, arg GetActiveProviderAccountForNewWorkParams) (OpenrailsPaymentProviderAccount, error) {
	row := q.db.QueryRow(ctx, getActiveProviderAccountForNewWork, arg.MerchantID, arg.Rail, arg.Environment)
	var i OpenrailsPaymentProviderAccount
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.DisplayName,
		&i.VaultSecretRef,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Owner,
		&i.Archived,
	)
	return i, err
}

const getProviderAccount = `-- name: GetProviderAccount :one
SELECT id, merchant_id, rail, environment, account_id, display_name, vault_secret_ref, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, owner, archived FROM openrails.payment_provider_accounts
WHERE id = $1
`

func (q *Queries) GetProviderAccount(ctx context.Context, id uuid.UUID) (OpenrailsPaymentProviderAccount, error) {
	row := q.db.QueryRow(ctx, getProviderAccount, id)
	var i OpenrailsPaymentProviderAccount
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.DisplayName,
		&i.VaultSecretRef,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Owner,
		&i.Archived,
	)
	return i, err
}

const getProviderAccountByIdentity = `-- name: GetProviderAccountByIdentity :one
SELECT id, merchant_id, rail, environment, account_id, display_name, vault_secret_ref, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, owner, archived FROM openrails.payment_provider_accounts
WHERE merchant_id = $1::uuid
  AND rail = lower($2::text)
  AND environment = COALESCE($3::text, 'live')
  AND account_id = $4::text
LIMIT 1
`

type GetProviderAccountByIdentityParams struct {
	MerchantID  uuid.UUID
	Rail        string
	Environment *string
	AccountID   string
}

func (q *Queries) GetProviderAccountByIdentity(ctx context.Context, arg GetProviderAccountByIdentityParams) (OpenrailsPaymentProviderAccount, error) {
	row := q.db.QueryRow(ctx, getProviderAccountByIdentity,
		arg.MerchantID,
		arg.Rail,
		arg.Environment,
		arg.AccountID,
	)
	var i OpenrailsPaymentProviderAccount
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.DisplayName,
		&i.VaultSecretRef,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Owner,
		&i.Archived,
	)
	return i, err
}

const getProviderAccountByRailIdentity = `-- name: GetProviderAccountByRailIdentity :one
SELECT id, merchant_id, rail, environment, account_id, display_name, vault_secret_ref, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, owner, archived FROM openrails.payment_provider_accounts
WHERE rail = lower($1::text)
  AND environment = COALESCE($2::text, 'live')
  AND account_id = $3::text
LIMIT 1
`

type GetProviderAccountByRailIdentityParams struct {
	Rail        string
	Environment *string
	AccountID   string
}

func (q *Queries) GetProviderAccountByRailIdentity(ctx context.Context, arg GetProviderAccountByRailIdentityParams) (OpenrailsPaymentProviderAccount, error) {
	row := q.db.QueryRow(ctx, getProviderAccountByRailIdentity, arg.Rail, arg.Environment, arg.AccountID)
	var i OpenrailsPaymentProviderAccount
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.DisplayName,
		&i.VaultSecretRef,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Owner,
		&i.Archived,
	)
	return i, err
}

const listProviderAccountsForMerchant = `-- name: ListProviderAccountsForMerchant :many
SELECT id, merchant_id, rail, environment, account_id, display_name, vault_secret_ref, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, owner, archived FROM openrails.payment_provider_accounts
WHERE merchant_id = $1::uuid
  AND ($2::text IS NULL OR rail = lower($2::text))
ORDER BY rail, environment, archived, created_at, id
`

type ListProviderAccountsForMerchantParams struct {
	MerchantID uuid.UUID
	Rail       *string
}

func (q *Queries) ListProviderAccountsForMerchant(ctx context.Context, arg ListProviderAccountsForMerchantParams) ([]OpenrailsPaymentProviderAccount, error) {
	rows, err := q.db.Query(ctx, listProviderAccountsForMerchant, arg.MerchantID, arg.Rail)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []OpenrailsPaymentProviderAccount
	for rows.Next() {
		var i OpenrailsPaymentProviderAccount
		if err := rows.Scan(
			&i.ID,
			&i.MerchantID,
			&i.Rail,
			&i.Environment,
			&i.AccountID,
			&i.DisplayName,
			&i.VaultSecretRef,
			&i.Evidence,
			&i.FirstSeenAt,
			&i.LastVerifiedAt,
			&i.ReplacedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Owner,
			&i.Archived,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertProviderAccount = `-- name: UpsertProviderAccount :one

INSERT INTO openrails.payment_provider_accounts (
    merchant_id, rail, environment, account_id, display_name,
    vault_secret_ref, archived, evidence, last_verified_at
) VALUES (
    $1::uuid,
    lower($2::text),
    COALESCE($3::text, 'live'),
    $4::text,
    $5,
    $6,
    COALESCE($7::boolean, false),
    $8,
    COALESCE($9::timestamptz, now())
)
ON CONFLICT (rail, environment, account_id) DO UPDATE SET
    display_name = COALESCE(EXCLUDED.display_name, openrails.payment_provider_accounts.display_name),
    vault_secret_ref = COALESCE(EXCLUDED.vault_secret_ref, openrails.payment_provider_accounts.vault_secret_ref),
    archived = EXCLUDED.archived,
    replaced_at = CASE
        WHEN EXCLUDED.archived THEN COALESCE(openrails.payment_provider_accounts.replaced_at, now())
        ELSE NULL
    END,
    evidence = COALESCE(EXCLUDED.evidence, openrails.payment_provider_accounts.evidence),
    last_verified_at = EXCLUDED.last_verified_at,
    updated_at = now()
WHERE openrails.payment_provider_accounts.merchant_id = EXCLUDED.merchant_id
RETURNING id, merchant_id, rail, environment, account_id, display_name, vault_secret_ref, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, owner, archived
`

type UpsertProviderAccountParams struct {
	MerchantID     uuid.UUID
	Rail           string
	Environment    *string
	AccountID      string
	DisplayName    *string
	VaultSecretRef *string
	Archived       *bool
	Evidence       []byte
	LastVerifiedAt *time.Time
}

// openrails.payment_provider_accounts: merchant-scoped provider account registry (#518).
func (q *Queries) UpsertProviderAccount(ctx context.Context, arg UpsertProviderAccountParams) (OpenrailsPaymentProviderAccount, error) {
	row := q.db.QueryRow(ctx, upsertProviderAccount,
		arg.MerchantID,
		arg.Rail,
		arg.Environment,
		arg.AccountID,
		arg.DisplayName,
		arg.VaultSecretRef,
		arg.Archived,
		arg.Evidence,
		arg.LastVerifiedAt,
	)
	var i OpenrailsPaymentProviderAccount
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.DisplayName,
		&i.VaultSecretRef,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Owner,
		&i.Archived,
	)
	return i, err
}
