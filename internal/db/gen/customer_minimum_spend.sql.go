// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: customer_minimum_spend.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const deleteCustomerMinimumSpend = `-- name: DeleteCustomerMinimumSpend :exec
DELETE FROM openrails.customer_minimum_spend
WHERE merchant_id = $1::uuid
  AND customer_id = $2::uuid
  AND currency = $3::text
`

type DeleteCustomerMinimumSpendParams struct {
	MerchantID uuid.UUID
	CustomerID uuid.UUID
	Currency   string
}

func (q *Queries) DeleteCustomerMinimumSpend(ctx context.Context, arg DeleteCustomerMinimumSpendParams) error {
	_, err := q.db.Exec(ctx, deleteCustomerMinimumSpend, arg.MerchantID, arg.CustomerID, arg.Currency)
	return err
}

const getCustomerMinimumSpend = `-- name: GetCustomerMinimumSpend :one

SELECT amount_micros
FROM openrails.customer_minimum_spend
WHERE merchant_id = $1::uuid
  AND customer_id = $2::uuid
  AND currency = $3::text
`

type GetCustomerMinimumSpendParams struct {
	MerchantID uuid.UUID
	CustomerID uuid.UUID
	Currency   string
}

// openrails.customer_minimum_spend (#643): per-customer per-currency minimum-spend
// commitment that trues-up at periodic invoice close.
func (q *Queries) GetCustomerMinimumSpend(ctx context.Context, arg GetCustomerMinimumSpendParams) (int64, error) {
	row := q.db.QueryRow(ctx, getCustomerMinimumSpend, arg.MerchantID, arg.CustomerID, arg.Currency)
	var amount_micros int64
	err := row.Scan(&amount_micros)
	return amount_micros, err
}

const upsertCustomerMinimumSpend = `-- name: UpsertCustomerMinimumSpend :exec
INSERT INTO openrails.customer_minimum_spend (
    merchant_id, customer_id, currency, amount_micros, created_at, updated_at
) VALUES (
    $1::uuid, $2::uuid, $3::text,
    $4::bigint, $5::timestamptz, $5::timestamptz
)
ON CONFLICT (merchant_id, customer_id, currency) DO UPDATE
    SET amount_micros = EXCLUDED.amount_micros, updated_at = EXCLUDED.updated_at
`

type UpsertCustomerMinimumSpendParams struct {
	MerchantID   uuid.UUID
	CustomerID   uuid.UUID
	Currency     string
	AmountMicros int64
	Now          time.Time
}

func (q *Queries) UpsertCustomerMinimumSpend(ctx context.Context, arg UpsertCustomerMinimumSpendParams) error {
	_, err := q.db.Exec(ctx, upsertCustomerMinimumSpend,
		arg.MerchantID,
		arg.CustomerID,
		arg.Currency,
		arg.AmountMicros,
		arg.Now,
	)
	return err
}
