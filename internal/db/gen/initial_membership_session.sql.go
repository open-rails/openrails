// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: initial_membership_session.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const completeInitialMembershipSession = `-- name: CompleteInitialMembershipSession :execrows
UPDATE openrails.checkout_sessions
SET status=$1::text,payment_id=$2::uuid,
    subscription_id=$3::uuid,transaction_id=$4::text,
    expires_at=NULL,updated_at=$5
WHERE merchant_id=$6::uuid AND id=$7::uuid
  AND customer_id=$8::uuid AND psp_id=$9::uuid
  AND price_id=$10::uuid AND rail=$11::text
  AND mode='subscription' AND deleted_at IS NULL
  AND rail_state ? 'initial_membership_quote'
  AND (status NOT IN ('succeeded','failed') OR status=$1::text)
`

type CompleteInitialMembershipSessionParams struct {
	Status         string
	PaymentID      *uuid.UUID
	SubscriptionID *uuid.UUID
	TransactionID  *string
	Now            time.Time
	MerchantID     uuid.UUID
	ID             uuid.UUID
	CustomerID     uuid.UUID
	PspID          uuid.UUID
	PriceID        uuid.UUID
	Rail           string
}

// The accepted operation owns its terminal checkout projection. Expiry is only
// an offer deadline and cannot erase an accepted financial operation.
func (q *Queries) CompleteInitialMembershipSession(ctx context.Context, arg CompleteInitialMembershipSessionParams) (int64, error) {
	result, err := q.db.Exec(ctx, completeInitialMembershipSession,
		arg.Status,
		arg.PaymentID,
		arg.SubscriptionID,
		arg.TransactionID,
		arg.Now,
		arg.MerchantID,
		arg.ID,
		arg.CustomerID,
		arg.PspID,
		arg.PriceID,
		arg.Rail,
	)
	if err != nil {
		return 0, err
	}
	return result.RowsAffected(), nil
}
