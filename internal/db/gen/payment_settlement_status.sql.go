// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: payment_settlement_status.sql

package gen

import (
	"context"

	"github.com/google/uuid"
)

const hasSettledPayment = `-- name: HasSettledPayment :one
SELECT EXISTS (
    SELECT 1 FROM openrails.payments
    WHERE merchant_id = $1::uuid
      AND customer_id = $2::uuid
      AND price_id = $3::uuid
      AND status IN ('completed', 'refunded')
      AND money_movement = 'rail'
      AND amount > 0
      AND refunded_payment_id IS NULL
)::boolean
`

type HasSettledPaymentParams struct {
	MerchantID uuid.UUID
	CustomerID uuid.UUID
	PriceID    uuid.UUID
}

// Durable settlement history, independent of host-event acknowledgment/retention.
func (q *Queries) HasSettledPayment(ctx context.Context, arg HasSettledPaymentParams) (bool, error) {
	row := q.db.QueryRow(ctx, hasSettledPayment, arg.MerchantID, arg.CustomerID, arg.PriceID)
	var column_1 bool
	err := row.Scan(&column_1)
	return column_1, err
}
