// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: rail_merchant_accounts.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const countActiveRailMerchantAccountsForNewWork = `-- name: CountActiveRailMerchantAccountsForNewWork :one
SELECT count(*)::bigint FROM openrails.rail_merchant_accounts
WHERE merchant_id = $1::uuid
  AND rail = lower($2::text)
  AND environment = COALESCE($3::text, 'live')
  AND archived = false
`

type CountActiveRailMerchantAccountsForNewWorkParams struct {
	MerchantID  uuid.UUID
	Rail        string
	Environment *string
}

func (q *Queries) CountActiveRailMerchantAccountsForNewWork(ctx context.Context, arg CountActiveRailMerchantAccountsForNewWorkParams) (int64, error) {
	row := q.db.QueryRow(ctx, countActiveRailMerchantAccountsForNewWork, arg.MerchantID, arg.Rail, arg.Environment)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const countRailMerchantAccountsForRailEnvironment = `-- name: CountRailMerchantAccountsForRailEnvironment :one
SELECT count(*)::bigint FROM openrails.rail_merchant_accounts
WHERE merchant_id = $1::uuid
  AND rail = lower($2::text)
  AND environment = COALESCE($3::text, 'live')
`

type CountRailMerchantAccountsForRailEnvironmentParams struct {
	MerchantID  uuid.UUID
	Rail        string
	Environment *string
}

func (q *Queries) CountRailMerchantAccountsForRailEnvironment(ctx context.Context, arg CountRailMerchantAccountsForRailEnvironmentParams) (int64, error) {
	row := q.db.QueryRow(ctx, countRailMerchantAccountsForRailEnvironment, arg.MerchantID, arg.Rail, arg.Environment)
	var column_1 int64
	err := row.Scan(&column_1)
	return column_1, err
}

const getActiveRailMerchantAccountForNewWork = `-- name: GetActiveRailMerchantAccountForNewWork :one
SELECT id, merchant_id, rail, environment, account_id, display_name, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, archived FROM openrails.rail_merchant_accounts
WHERE merchant_id = $1::uuid
  AND rail = lower($2::text)
  AND environment = COALESCE($3::text, 'live')
  AND archived = false
ORDER BY created_at DESC, id DESC
LIMIT 1
`

type GetActiveRailMerchantAccountForNewWorkParams struct {
	MerchantID  uuid.UUID
	Rail        string
	Environment *string
}

// The newest non-archived account on a rail+environment. Existing provider-bound
// work must use its recorded rail_merchant_account_id instead of this selector.
func (q *Queries) GetActiveRailMerchantAccountForNewWork(ctx context.Context, arg GetActiveRailMerchantAccountForNewWorkParams) (OpenrailsRailMerchantAccount, error) {
	row := q.db.QueryRow(ctx, getActiveRailMerchantAccountForNewWork, arg.MerchantID, arg.Rail, arg.Environment)
	var i OpenrailsRailMerchantAccount
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.DisplayName,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Archived,
	)
	return i, err
}

const getRailMerchantAccount = `-- name: GetRailMerchantAccount :one
SELECT id, merchant_id, rail, environment, account_id, display_name, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, archived FROM openrails.rail_merchant_accounts
WHERE id = $1
`

func (q *Queries) GetRailMerchantAccount(ctx context.Context, id uuid.UUID) (OpenrailsRailMerchantAccount, error) {
	row := q.db.QueryRow(ctx, getRailMerchantAccount, id)
	var i OpenrailsRailMerchantAccount
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.DisplayName,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Archived,
	)
	return i, err
}

const getRailMerchantAccountByIdentity = `-- name: GetRailMerchantAccountByIdentity :one
SELECT id, merchant_id, rail, environment, account_id, display_name, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, archived FROM openrails.rail_merchant_accounts
WHERE merchant_id = $1::uuid
  AND rail = lower($2::text)
  AND environment = COALESCE($3::text, 'live')
  AND account_id = $4::text
LIMIT 1
`

type GetRailMerchantAccountByIdentityParams struct {
	MerchantID  uuid.UUID
	Rail        string
	Environment *string
	AccountID   string
}

func (q *Queries) GetRailMerchantAccountByIdentity(ctx context.Context, arg GetRailMerchantAccountByIdentityParams) (OpenrailsRailMerchantAccount, error) {
	row := q.db.QueryRow(ctx, getRailMerchantAccountByIdentity,
		arg.MerchantID,
		arg.Rail,
		arg.Environment,
		arg.AccountID,
	)
	var i OpenrailsRailMerchantAccount
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.DisplayName,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Archived,
	)
	return i, err
}

const getRailMerchantAccountByRailIdentity = `-- name: GetRailMerchantAccountByRailIdentity :one
SELECT id, merchant_id, rail, environment, account_id, display_name, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, archived FROM openrails.rail_merchant_accounts
WHERE rail = lower($1::text)
  AND environment = COALESCE($2::text, 'live')
  AND account_id = $3::text
LIMIT 1
`

type GetRailMerchantAccountByRailIdentityParams struct {
	Rail        string
	Environment *string
	AccountID   string
}

func (q *Queries) GetRailMerchantAccountByRailIdentity(ctx context.Context, arg GetRailMerchantAccountByRailIdentityParams) (OpenrailsRailMerchantAccount, error) {
	row := q.db.QueryRow(ctx, getRailMerchantAccountByRailIdentity, arg.Rail, arg.Environment, arg.AccountID)
	var i OpenrailsRailMerchantAccount
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.DisplayName,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Archived,
	)
	return i, err
}

const listRailMerchantAccountsForMerchant = `-- name: ListRailMerchantAccountsForMerchant :many
SELECT id, merchant_id, rail, environment, account_id, display_name, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, archived FROM openrails.rail_merchant_accounts
WHERE merchant_id = $1::uuid
  AND ($2::text IS NULL OR rail = lower($2::text))
ORDER BY rail, environment, archived, created_at, id
`

type ListRailMerchantAccountsForMerchantParams struct {
	MerchantID uuid.UUID
	Rail       *string
}

func (q *Queries) ListRailMerchantAccountsForMerchant(ctx context.Context, arg ListRailMerchantAccountsForMerchantParams) ([]OpenrailsRailMerchantAccount, error) {
	rows, err := q.db.Query(ctx, listRailMerchantAccountsForMerchant, arg.MerchantID, arg.Rail)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []OpenrailsRailMerchantAccount
	for rows.Next() {
		var i OpenrailsRailMerchantAccount
		if err := rows.Scan(
			&i.ID,
			&i.MerchantID,
			&i.Rail,
			&i.Environment,
			&i.AccountID,
			&i.DisplayName,
			&i.Evidence,
			&i.FirstSeenAt,
			&i.LastVerifiedAt,
			&i.ReplacedAt,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Archived,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const upsertRailMerchantAccount = `-- name: UpsertRailMerchantAccount :one

INSERT INTO openrails.rail_merchant_accounts (
    id, merchant_id, rail, environment, account_id, display_name,
    archived, evidence, last_verified_at
) VALUES (
    $1::uuid,
    $2::uuid,
    lower($3::text),
    COALESCE($4::text, 'live'),
    $5::text,
    $6,
    COALESCE($7::boolean, false),
    $8,
    COALESCE($9::timestamptz, now())
)
ON CONFLICT (rail, environment, account_id) DO UPDATE SET
    display_name = COALESCE(EXCLUDED.display_name, openrails.rail_merchant_accounts.display_name),
    archived = EXCLUDED.archived,
    replaced_at = CASE
        WHEN EXCLUDED.archived THEN COALESCE(openrails.rail_merchant_accounts.replaced_at, now())
        ELSE NULL
    END,
    evidence = COALESCE(EXCLUDED.evidence, openrails.rail_merchant_accounts.evidence),
    last_verified_at = EXCLUDED.last_verified_at,
    updated_at = now()
WHERE openrails.rail_merchant_accounts.merchant_id = EXCLUDED.merchant_id
RETURNING id, merchant_id, rail, environment, account_id, display_name, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, archived
`

type UpsertRailMerchantAccountParams struct {
	ID             uuid.UUID
	MerchantID     uuid.UUID
	Rail           string
	Environment    *string
	AccountID      string
	DisplayName    *string
	Archived       *bool
	Evidence       []byte
	LastVerifiedAt *time.Time
}

// openrails.rail_merchant_accounts: merchant-scoped provider account registry (#518).
func (q *Queries) UpsertRailMerchantAccount(ctx context.Context, arg UpsertRailMerchantAccountParams) (OpenrailsRailMerchantAccount, error) {
	row := q.db.QueryRow(ctx, upsertRailMerchantAccount,
		arg.ID,
		arg.MerchantID,
		arg.Rail,
		arg.Environment,
		arg.AccountID,
		arg.DisplayName,
		arg.Archived,
		arg.Evidence,
		arg.LastVerifiedAt,
	)
	var i OpenrailsRailMerchantAccount
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.DisplayName,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Archived,
	)
	return i, err
}
