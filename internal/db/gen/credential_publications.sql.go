// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: credential_publications.sql

package gen

import (
	"context"

	"github.com/google/uuid"
)

const completeCredentialPublication = `-- name: CompleteCredentialPublication :exec
UPDATE openrails.credential_publications
SET state = 'published', result = $3, published_at = now()
WHERE merchant_id = $1 AND operation_id = $2
`

type CompleteCredentialPublicationParams struct {
	MerchantID  uuid.UUID
	OperationID uuid.UUID
	Result      []byte
}

func (q *Queries) CompleteCredentialPublication(ctx context.Context, arg CompleteCredentialPublicationParams) error {
	_, err := q.db.Exec(ctx, completeCredentialPublication, arg.MerchantID, arg.OperationID, arg.Result)
	return err
}

const createCredentialPublication = `-- name: CreateCredentialPublication :exec

INSERT INTO openrails.credential_publications (
    merchant_id, operation_id, rail, environment, account_id, expected_revision, request_metadata
) VALUES ($1, $2, $3, $4, $5, $6, $7)
ON CONFLICT (merchant_id, operation_id) DO NOTHING
`

type CreateCredentialPublicationParams struct {
	MerchantID       uuid.UUID
	OperationID      uuid.UUID
	Rail             string
	Environment      string
	AccountID        string
	ExpectedRevision int64
	RequestMetadata  []byte
}

// Credential publication receipts contain metadata and references, never secrets.
func (q *Queries) CreateCredentialPublication(ctx context.Context, arg CreateCredentialPublicationParams) error {
	_, err := q.db.Exec(ctx, createCredentialPublication,
		arg.MerchantID,
		arg.OperationID,
		arg.Rail,
		arg.Environment,
		arg.AccountID,
		arg.ExpectedRevision,
		arg.RequestMetadata,
	)
	return err
}

const getCredentialPublication = `-- name: GetCredentialPublication :one
SELECT rail, environment, account_id, expected_revision, request_metadata, result
FROM openrails.credential_publications
WHERE merchant_id = $1 AND operation_id = $2
`

type GetCredentialPublicationParams struct {
	MerchantID  uuid.UUID
	OperationID uuid.UUID
}

type GetCredentialPublicationRow struct {
	Rail             string
	Environment      string
	AccountID        string
	ExpectedRevision int64
	RequestMetadata  []byte
	Result           []byte
}

func (q *Queries) GetCredentialPublication(ctx context.Context, arg GetCredentialPublicationParams) (GetCredentialPublicationRow, error) {
	row := q.db.QueryRow(ctx, getCredentialPublication, arg.MerchantID, arg.OperationID)
	var i GetCredentialPublicationRow
	err := row.Scan(
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.ExpectedRevision,
		&i.RequestMetadata,
		&i.Result,
	)
	return i, err
}

const lockCredentialPublication = `-- name: LockCredentialPublication :one
SELECT rail, environment, account_id, expected_revision, request_metadata, result
FROM openrails.credential_publications
WHERE merchant_id = $1 AND operation_id = $2
FOR UPDATE
`

type LockCredentialPublicationParams struct {
	MerchantID  uuid.UUID
	OperationID uuid.UUID
}

type LockCredentialPublicationRow struct {
	Rail             string
	Environment      string
	AccountID        string
	ExpectedRevision int64
	RequestMetadata  []byte
	Result           []byte
}

func (q *Queries) LockCredentialPublication(ctx context.Context, arg LockCredentialPublicationParams) (LockCredentialPublicationRow, error) {
	row := q.db.QueryRow(ctx, lockCredentialPublication, arg.MerchantID, arg.OperationID)
	var i LockCredentialPublicationRow
	err := row.Scan(
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.ExpectedRevision,
		&i.RequestMetadata,
		&i.Result,
	)
	return i, err
}

const lockCredentialPublicationResult = `-- name: LockCredentialPublicationResult :one
SELECT result FROM openrails.credential_publications
WHERE merchant_id = $1 AND operation_id = $2
FOR UPDATE
`

type LockCredentialPublicationResultParams struct {
	MerchantID  uuid.UUID
	OperationID uuid.UUID
}

func (q *Queries) LockCredentialPublicationResult(ctx context.Context, arg LockCredentialPublicationResultParams) ([]byte, error) {
	row := q.db.QueryRow(ctx, lockCredentialPublicationResult, arg.MerchantID, arg.OperationID)
	var result []byte
	err := row.Scan(&result)
	return result, err
}

const lockPSPForCredentialPublication = `-- name: LockPSPForCredentialPublication :one
SELECT id, merchant_id, rail, environment, account_id, key, evidence, first_seen_at, last_verified_at, replaced_at, created_at, updated_at, archived, custodian_id, pending_signer_public_key FROM openrails.psps
WHERE merchant_id = $1 AND rail = $2 AND environment = $3 AND account_id = $4
FOR UPDATE
`

type LockPSPForCredentialPublicationParams struct {
	MerchantID  uuid.UUID
	Rail        string
	Environment string
	AccountID   string
}

func (q *Queries) LockPSPForCredentialPublication(ctx context.Context, arg LockPSPForCredentialPublicationParams) (OpenrailsPsp, error) {
	row := q.db.QueryRow(ctx, lockPSPForCredentialPublication,
		arg.MerchantID,
		arg.Rail,
		arg.Environment,
		arg.AccountID,
	)
	var i OpenrailsPsp
	err := row.Scan(
		&i.ID,
		&i.MerchantID,
		&i.Rail,
		&i.Environment,
		&i.AccountID,
		&i.Key,
		&i.Evidence,
		&i.FirstSeenAt,
		&i.LastVerifiedAt,
		&i.ReplacedAt,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Archived,
		&i.CustodianID,
		&i.PendingSignerPublicKey,
	)
	return i, err
}
